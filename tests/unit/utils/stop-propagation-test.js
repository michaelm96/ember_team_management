import stopPropagation from 'ember-team-management/utils/stop-propagation';
import { module, test } from 'qunit';

module('Unit | Utility | stop-propagation', function () {
  // TODO: Replace this with your real tests.
  test('it works', function (assert) {
    let result = stopPropagation();
    assert.ok(result);
  });
});
